---
id: mapKeys
---

<!-- Do not edit this file. It is generated automatically -->

## mapKeys() function

`obj` と同じ値を持ち、各キーが元の値とキーに `f` を適用した結果である新しいオブジェクトを返します。複数のキーが同じ新しいキーに変換される場合、最後のキーの値が残ります。

**Signature:**

```typescript
function mapKeys<T extends object, K2 extends PropertyKey>(
  f: (value: T[keyof T], key: keyof T & string) => K2,
  obj: T,
): Record<K2, T[keyof T]>;
```

## Example

```ts
mapKeys((v, k) => k.toUpperCase(), { a: 1, b: 2 }); // { A: 1, B: 2 }
mapKeys((v) => String(v), { a: 1 }); // { "1": 1 }

await mapKeys(async (v, k) => k.toUpperCase(), { a: 1 }); // { A: 1 }

pipe(
  { a: 1, b: 2 },
  mapKeys((v, k) => k.toUpperCase()),
); // { A: 1, B: 2 }
```

[Open Source Code](https://github.com/marpple/FxTS/blob/main/src/mapKeys.ts)
