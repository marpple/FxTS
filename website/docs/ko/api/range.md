---
id: range
---

<!-- Do not edit this file. It is generated automatically -->

## range() function

start부터 end까지(end는 포함하지 않음) 진행되는 숫자(양수 및/또는 음수)의 Iterable/AsyncIterable을 반환합니다. start로 시작하도록 설정한 다음 0으로 설정됩니다.

**Signature:**

```typescript
declare function range(length: number): IterableIterator<number>;
```

## Example

```ts
const iter = range(4);
iter.next(); // {done:false, value: 0}
iter.next(); // {done:false, value: 1}
iter.next(); // {done:false, value: 2}
iter.next(); // {done:false, value: 3}
iter.next(); // {done:true, value: undefined}

// with pipe
pipe(range(4), toArray); // [0, 1, 2, 3]

pipe(range(1, 4), toArray); // [1, 2, 3]

// with toAsync
await pipe(range(4), toAsync, toArray); // [0, 1, 2, 3]
```

[Try It](https://codesandbox.io/s/fxts-range-3phyd)

see [pipe](https://fxts.dev/ko/api/pipe)<!-- -->, [toAsync](https://fxts.dev/ko/api/toAsync)<!-- -->, [toArray](https://fxts.dev/ko/api/toArray)

[Open Source Code](https://github.com/marpple/FxTS/blob/main/src/Lazy/range.ts)
