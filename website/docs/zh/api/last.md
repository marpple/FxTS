---
id: last
---

<!-- Do not edit this file. It is generated automatically -->

## last() function

返回 Iterable/AsyncIterable 的最后一个元素。

**Signature:**

```typescript
declare function last<T extends Iterable<unknown> | AsyncIterable<unknown>>(
  iterable: T,
): LastReturnType<T>;
```

## Example

```ts
last([1, 2, 3, 4, 5]); // 5

// with pipe
pipe([1, 2, 3, 4, 5], last); // 5

await pipe(Promise.resolve([1, 2, 3, 4, 5]), last); // 5

// with toAsync
await pipe(
  [Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)],
  toAsync,
  last,
); // 3
```

[Try It](https://codesandbox.io/s/fxts-last-zbvq8)

see [pipe](https://fxts.dev/zh/api/pipe)<!-- -->, [toAsync](https://fxts.dev/zh/api/toAsync)

[Open Source Code](https://github.com/marpple/FxTS/blob/main/src/last.ts)
