---
id: mapValues
---

<!-- Do not edit this file. It is generated automatically -->

## mapValues() function

返回一个与 `obj` 具有相同键的新对象,其中每个值都是对原值(及其键)应用 `f` 的结果。使用异步回调时返回 `Promise`。

**Signature:**

```typescript
function mapValues<T extends object, B>(
  f: (value: T[keyof T], key: keyof T) => B,
  obj: T,
): { [K in keyof T]: B };
```

## Example

```ts
mapValues((v) => v * 2, { a: 1, b: 2 }); // { a: 2, b: 4 }
mapValues((v, k) => `${k}:${v}`, { a: 1 }); // { a: "a:1" }

await mapValues(async (v) => v * 2, { a: 1, b: 2 }); // { a: 2, b: 4 }

pipe(
  { a: 1, b: 2 },
  mapValues((v) => v * 2),
); // { a: 2, b: 4 }
```

[Open Source Code](https://github.com/marpple/FxTS/blob/main/src/mapValues.ts)
